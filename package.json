{
  "name": "whatsapp-agent",
  "version": "0.1.0",
  "description": "Personal WhatsApp automation agent using Baileys, an allowlist, and optional OpenAI replies.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/ai.js && node --check src/knowledge.js && node --check src/registry.js && node --check src/server.js && node --check src/text.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "^6.7.18",
    "dotenv": "^16.4.7",
    "pino": "^9.6.0",
    "qrcode": "^1.5.4",
    "qrcode-terminal": "^0.12.0"
  }
}
